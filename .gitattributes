# Git LFS Configuration for Claude Code Workflow
# These patterns will automatically track large files with Git LFS

# Data files (ONLY large data files)
# *.csv filter=lfs diff=lfs merge=lfs -text  # Disabled for template compatibility
# *.tsv filter=lfs diff=lfs merge=lfs -text  # Disabled for template compatibility
# *.json filter=lfs diff=lfs merge=lfs -text # Disabled for template compatibility
# Scientific data formats (large files only)
*.h5 filter=lfs diff=lfs merge=lfs -text
*.hdf5 filter=lfs diff=lfs merge=lfs -text
*.pkl filter=lfs diff=lfs merge=lfs -text
*.pickle filter=lfs diff=lfs merge=lfs -text
# Large binary files (typically > 100MB)
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
# Model files (typically large)
*.model filter=lfs diff=lfs merge=lfs -text
*.joblib filter=lfs diff=lfs merge=lfs -text
# Explicitly EXCLUDE config and documentation files from LFS
.claude/**/*.json -filter
docs/**/*.md -filter
*.md -filter
.env.example -filter
README.md -filter
CLAUDE.md -filter
*.py -filter
*.sh -filter
*.json !text !filter !merge !diff
